/** @type {import('next').NextConfig} */
const nextConfig = {
  // We'll add the export config back when deploying to GitHub Pages
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
  // basePath: '/cardano-parameters',
}

module.exports = nextConfig 